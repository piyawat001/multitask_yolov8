task: segment,classify
mode: train

# ส่วนของ segmentation
seg_data: path/to/segmentation/data.yaml
seg_model: yolov8n-seg.yaml

# ส่วนของ classification
cls_data: path/to/classification/data.yaml
cls_model: yolov8n-cls.yaml

# พารามิเตอร์การเทรน
epochs: 1000
batch: 16
imgsz: 640
patience: 20
